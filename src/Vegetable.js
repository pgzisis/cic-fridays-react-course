// Here in this component first we should pass the props object in the required parameters.
// After doing so, we just have to replace the *I want to display a veggie here* with the `name` prop.

// **Hint If stuck, you can look back on the props branch.

const Vegetable = (props) => {
  return <p>I want to display a veggie here</p>;
};

export { Vegetable };

// When you are finished go back to the App.js file
